# CV - Lonnie Mac McBride II

Professional CV styled for defense technology consulting.

## Usage

1. Save your headshot as `profile.jpg` in this directory
2. Open `index.html` in a browser
3. Print to PDF if needed

## GitHub Pages

To host this as a live page, enable GitHub Pages in repo settings pointing to the `main` branch (or whichever branch contains `index.html`).
